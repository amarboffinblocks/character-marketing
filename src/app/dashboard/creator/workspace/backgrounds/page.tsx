import { DashboardPlaceholderState } from "@/features/creator/dashboard/components/dashboard-placeholder-state"

export default function CreatorWorkspaceBackgroundsPage() {
  return (
    <DashboardPlaceholderState
      title="Workspace · Backgrounds"
      description="Maintain reusable background references and finalized scene assets."
    />
  )
}
